<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :umbrella_id %><br>
    <%= f.text_field :umbrella_id %>
  </div>
  <div class="field">
    <%= f.label :gps_present_lat %><br>
    <%= f.text_field :gps_present_lat %>
  </div>
  <div class="field">
    <%= f.label :gps_present_lon %><br>
    <%= f.text_field :gps_present_lon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
